# These are optional dependencies
# nvshmem4py is aware of and interoperable with them
# but doesn't depend on them
mpi4py
--index-url https://download.pytorch.org/whl/cu130
--extra-index-url https://pypi.org/simple
torch==2.12.0+cu130
--index-url https://pypi.org/simple
ml-dtypes
# These are cu13 packages
nvidia-cuda-nvcc
nvidia-nvjitlink
# CUDA 13 toolkit for numbast header search at runtime
cuda-toolkit[cudart,nvcc,curand,cccl,nvvm]==13.*